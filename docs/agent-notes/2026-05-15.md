# 2026-05-15 Agent Notes

## Purpose

- Record implementation findings that are easy to forget and likely to recur.
- Prefer short, searchable notes that point to working code paths and known platform constraints.

## Feishu attachment submission card: input max length is capped by the platform

- Symptom:
  - The attachment reply card is created through CardKit, but Feishu returns business error `11310` when the card contains `form.input.max_length` above the supported default limit.
- Confirmed behavior:
  - For this form input path, Feishu rejected `max_length = 4000` with:
    - `ErrMsg: max_length exceed the default maximum 1000`
- Working rule:
  - Keep attachment supplemental text inputs at `1000` characters or below.
- Current implementation:
  - Shared constants live in [FeishuAttachmentSubmissionDefaults.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Model/FeishuAttachmentSubmissionDefaults.cs:1).
  - Card payload uses that limit in [FeishuAttachmentSubmissionCardHelper.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Service/Channels/FeishuAttachmentSubmissionCardHelper.cs:1).
  - Server-side submit validation also enforces it in [FeishuCardActionService.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Service/Channels/FeishuCardActionService.cs:624).
- Search hints:
  - `max_length exceed the default maximum 1000`
  - `submit_attachment_prompt`
  - `feishu_attachment_prompt`

## Feishu form-submit buttons should follow the existing Superpowers pattern

- Symptom:
  - A reply attachment card can be created successfully, but clicking the submit button may not produce any `card.action.trigger` callback in WebCode logs.
- Likely cause:
  - The button shape drifted away from an already working Feishu form-submit pattern used elsewhere in the codebase.
- Working reference:
  - Reuse the same structure as the Superpowers bottom prompt submit button in [FeishuCardKitClient.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Service/Channels/FeishuCardKitClient.cs:687).
- Required button fields for this path:
  - `action_type = "form_submit"`
  - a stable `name`
  - a `value` payload carrying the action metadata
- Current attachment implementation:
  - Button constants live in [FeishuAttachmentSubmissionDefaults.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Model/FeishuAttachmentSubmissionDefaults.cs:1).
  - Attachment card builder is in [FeishuAttachmentSubmissionCardHelper.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Service/Channels/FeishuAttachmentSubmissionCardHelper.cs:1).
- Search hints:
  - `SubmitButtonName`
  - `form_submit`
  - `BuildBottomPromptForm`
  - `card.action.trigger`

## Feishu card callback debugging checklist

- If a card renders but clicking it appears to do nothing:
  - First confirm whether WebCode logs contain `🔥 [FeishuCard] 收到卡片回调事件`.
  - If not, inspect the card JSON shape before blaming the CLI execution path.
  - Compare the button and form structure with an already working in-repo Feishu card implementation before inventing a new shape.
- Current callback entry:
  - [FeishuCardActionHandler.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Service/Channels/FeishuCardActionHandler.cs:1)

## Feishu pasted inline images arrive as `post`, not top-level `image`

- Symptom:
  - A user pastes an image directly into Feishu's rich-text input box and sends text with it.
  - WebCode logs `MessageType=post`, and the normalized prompt becomes something like:
    - `[image]`
    - `这是啥？`
  - If this path is treated as plain text, CLI only sees the placeholder string and not the actual image bytes.
- Confirmed behavior:
  - This input does not use the same payload shape as top-level `image` messages.
  - The real image key is nested under rich-text nodes such as:
    - `content[][ { "tag": "img", "image_key": "..." } ]`
- Working rule:
  - Parse inline `img` nodes from Feishu `post` payloads into `FeishuIncomingMessage.Attachments`.
  - When a `post` message contains both inline image attachments and real text, bypass the attachment submission card and submit the text plus attachment directly to CLI through the prepared-submission path.
  - Keep the text prompt clean by stripping placeholder markers like `[image]` and `[media]` before building the CLI request.
  - Do not download pasted inline images through `/open-apis/im/v1/images/{image_key}`. For this path, use the message resource API with the original `messageId`, `image_key`, and `type=image`.
- Current implementation:
  - Inline rich-text attachment parsing lives in [FeishuIncomingAttachmentParser.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Service/Channels/FeishuIncomingAttachmentParser.cs:1).
  - Direct `post` attachment submission logic lives in [FeishuChannelService.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Service/Channels/FeishuChannelService.cs:1).
  - Normalization of Feishu rich-text content still lives in [FeishuPromptNormalizer.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Service/Channels/FeishuPromptNormalizer.cs:1).
- Search hints:
  - `MessageType=post`
  - `tag\":\"img`
  - `TryHandleInlinePostSubmissionAsync`
  - `[image]`

## Singleton channel services must not resolve scoped submission services from the root container

- Symptom:
  - A Feishu message path works in tests or partial flows, but runtime throws:
    - `Cannot resolve scoped service '...IMessageSubmissionService' from root provider.`
- Root cause:
  - `FeishuChannelService` is registered as a singleton.
  - `IMessageSubmissionService` and `IAttachmentStagingService` are scoped.
  - Calling `GetService<T>()` for those directly on the root provider is invalid under the default DI container.
- Working rule:
  - In singleton message/channel services, create a scope first, then resolve scoped services from `scope.ServiceProvider`.
  - If a fallback manual construction path is needed, also resolve its dependencies from that scope.
- Current implementation:
  - Inline post attachment submission now prepares the draft through a scoped helper in [FeishuChannelService.cs](/D:/VSWorkshop/WebCode/WebCodeCli.Domain/Domain/Service/Channels/FeishuChannelService.cs:710).
- Search hints:
  - `Cannot resolve scoped service`
  - `CreateScope()`
  - `PrepareMessageSubmissionAsync`

## Windows packaging script should not round-trip appsettings through PowerShell ConvertTo/From-Json

- Symptom:
  - `tools/build-windows-installer.ps1` succeeds through `dotnet publish`, but later fails while reading the published `appsettings.json` for release notes.
  - The error shows `ConvertFrom-Json` rejecting the generated file, even though the source `WebCodeCli/appsettings.json` is valid.
- Root cause:
  - The packaging script used PowerShell's `ConvertFrom-Json` + `ConvertTo-Json` to rewrite `publish/appsettings.json`.
  - On this repo's CLI tool comments, the round-trip can emit invalid JSON by losing escaping around embedded quotes such as `"{prompt}"` inside tool argument comment strings.
- Working rule:
  - For release packaging on Windows PowerShell, do not rewrite `appsettings.json` with `ConvertTo-Json`.
  - Use a serializer that preserves embedded quotes correctly, such as `System.Web.Script.Serialization.JavaScriptSerializer`, for both reading and writing in the packaging script.
- Current implementation:
  - The serializer helpers and publish-settings rewrite live in [build-windows-installer.ps1](/D:/VSWorkshop/WebCode/tools/build-windows-installer.ps1:1).
- Search hints:
  - `Get-LaunchUrlForReleaseNotes`
  - `Update-PublishAppSettings`
  - `JavaScriptSerializer`
  - `"{prompt}"`
